# Prompts para Tareas de Documentación

## Documentar una función con JSDoc

"Añade documentación en formato JSDoc a la función `safeFetch` en el archivo `Daring Dungeon Delver/src/utils/safeFetch.ts`. La documentación debe explicar qué hace la función, qué parámetros acepta (`url` y `options`), qué devuelve, y qué tipo de error puede lanzar (`FetchError`)."

## Actualizar un documento Markdown

"Actualiza el archivo `docs/AGENTS/ARCHITECTURE.md`. Añade una nueva sección llamada 'Gestión de Estado' y explica brevemente que el proyecto usa Pinia. Menciona que los stores se encuentran en `Daring Dungeon Delver/src/stores/` y enlaza al archivo `scoreStore.ts` como ejemplo."

## Generar documentación de un componente

"Genera una breve documentación en formato Markdown para el componente `GameView.vue` que se encuentra en `Daring Dungeon Delver/src/views/ddd/GameView.vue`. Explica su propósito principal (inicializar y montar el juego de Phaser), qué props acepta y qué eventos emite."
